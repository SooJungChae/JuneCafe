// 테스트서버에서 커피 메뉴를 가져온다.
const editCoffeeMenu = async () => {
  // 수정하는건 작업하세요 ~~
};

// 페이지 로드되면 바로 실행한다.
(() => {
  $('#summernote').summernote({
    // 위에 팝업생기는거 없앰
    popover: {
      image: [],
    },
    height: 300
  });
})();